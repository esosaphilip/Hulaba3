package com.example.hulaba3.data.database

enum class PreferredLearningTime {
    Morning, Afternoon, Evening, Night
}

enum class LearningGoal {
    Vocabulary, Grammar, Speaking, Writing, Listening, Reading, ExamPrep
}

enum class AuthProvider {
    EmailPassword, Google, Facebook, Apple, Github
}